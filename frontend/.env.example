# The frontend currently uses a hardcoded API key in `src/App.jsx`.
# If you prefer to use an environment variable instead, change the code to read
# `import.meta.env.VITE_OPENWEATHER_API_KEY` and set that variable here.

